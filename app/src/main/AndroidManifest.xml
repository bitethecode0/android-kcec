<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joon.kcec">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_kcec_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_kcec_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginNRegister.LoginActivity"></activity>
        <activity android:name=".LoginNRegister.RegisterActivity" />
        <activity android:name=".AccountSettings.AccountActivity" />

        <!-- activies in drawer menu -->
        <activity android:name=".Announcement.AnnoucementActivity" />
        <activity android:name=".Documents.DocsActivity" />
        <activity android:name=".Gallery.GalleryActivity" />
        <activity android:name=".QuestionAnswer.QandAActivity" />
        <activity android:name=".Events.EventsActivity" />
        <activity android:name=".Gallery.NextActivity" />
        <activity android:name=".Gallery.ShowSelectedPhotosActivity"></activity>
    </application>

</manifest>